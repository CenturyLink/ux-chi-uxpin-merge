import React from 'react';
import SearchInput from '../Search-input';

export default (
  <SearchInput
    uxpId="search-input"
    placeholder="Search"
    helperMessageState="default"
    value=""
    size="md"
    disabled={false}
    clear={() => {}}
    click={() => {}}
    focus={() => {}}
    focusLost={() => {}}
    input={() => {}}
    valueChange={() => {}}
    search={() => {}}
  />
);
