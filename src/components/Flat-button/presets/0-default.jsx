import React from 'react';
import FlatButton from '../Flat-button';

export default (
  <FlatButton
    uxpId="flat-button"
    disabled={false}
    size="md"
    value="Flat"
    fluid={false}
    click={() => {}}
    mouseDown={() => {}}
    mouseUp={() => {}}
    mouseOver={() => {}}
    mouseLeave={() => {}}
  />
);
