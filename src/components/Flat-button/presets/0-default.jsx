import React from 'react';
import FlatButton from '../Flat-button';

export default (
  <FlatButton
    uxpId="primary-button"
    disabled={false}
    leftIcon=""
    rightIcon=""
    size="md"
    value="Flat"
    fluid={false}
    click={() => {}}
    mouseDown={() => {}}
    mouseUp={() => {}}
    mouseOver={() => {}}
    mouseLeave={() => {}}
  />
);
