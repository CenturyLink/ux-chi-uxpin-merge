import React from 'react';
import SecondaryButton from '../Secondary-button';

export default (
  <SecondaryButton
    uxpId="secondary-button"
    disabled={false}
    leftIcon=""
    rightIcon=""
    size="lg"
    value="Secondary"
    fluid={false}
    click={() => {}}
    mouseDown={() => {}}
    mouseUp={() => {}}
    mouseOver={() => {}}
    mouseLeave={() => {}}
  />
);
