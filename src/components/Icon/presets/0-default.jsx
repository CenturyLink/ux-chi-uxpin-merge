import React from 'react';
import Icon from '../Icon';

export default (
  <Icon
    uxpId="icon"
    size="sm"
    icon="atom"
    color="primary"
    click={() => {}}
    mouseDown={() => {}}
    mouseLeave={() => {}}
    mouseOver={() => {}}
    mouseUp={() => {}}
  />
);
