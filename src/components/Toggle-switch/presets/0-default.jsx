import React from 'react';
import ToggleSwitch from '../Toggle-switch';

export default (
  <ToggleSwitch
    uxpId="toggle-switch"
    size="sm"
    label="Label"
    toggleLabel="Toggle Label"
    on={false}
    required="none"
    disabled={false}
    info={false}
  />
);
