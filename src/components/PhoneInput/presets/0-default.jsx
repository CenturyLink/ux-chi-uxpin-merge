import React from 'react';
import PhoneInput from '../PhoneInput';

export default (
  <PhoneInput
    uxpId="phoneInput"
    countryCode="1"
    click={() => {}}
    info={false}
    label="Phone Number"
    mouseLeave={() => {}}
    mouseOver={() => {}}
    input={() => {}}
    required="none"
    size="md"
    valueChange={() => {}}
  />
);
