import React from 'react';
import NumberInput from '../Number-input';

export default (
  <NumberInput
    uxpId="number-input"
    size="md"
    expanded={false}
    label="Label"
    required="none"
    disabled={false}
    startValue="0"
    min={1}
    max={1}
    click={() => {}}
    focus={() => {}}
    focusLost={() => {}}
    input={() => {}}
    mouseDown={() => {}}
    mouseLeave={() => {}}
    mouseOver={() => {}}
    mouseUp={() => {}}
    valueChange={() => {}}
  />
);
