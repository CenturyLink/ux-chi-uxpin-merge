import React from 'react';
import IconButton from '../Icon-button';

export default (
  <IconButton
    uxpId="icon-button"
    disabled={false}
    icon="atom"
    click={() => {}}
    tooltipMessage="Tooltip message"
    tooltipPosition="top"
    tooltipColor="base"
  />
);
