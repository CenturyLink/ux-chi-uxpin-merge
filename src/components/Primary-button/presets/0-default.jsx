import React from 'react';
import PrimaryButton from '../Primary-button';

export default (
  <PrimaryButton
    uxpId="primary-button"
    disabled={false}
    leftIcon=""
    rightIcon=""
    size="md"
    value="Primary"
    fluid={false}
    click={() => {}}
    mouseDown={() => {}}
    mouseUp={() => {}}
    mouseOver={() => {}}
    mouseLeave={() => {}}
  />
);
