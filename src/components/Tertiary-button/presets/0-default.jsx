import React from 'react';
import TertiaryButton from '../Tertiary-button';

export default (
  <TertiaryButton
    uxpId="tertiary-button"
    disabled={false}
    leftIcon=""
    rightIcon=""
    size="xs"
    value="Tertiary"
    fluid={false}
    click={() => {}}
    mouseDown={() => {}}
    mouseUp={() => {}}
    mouseOver={() => {}}
    mouseLeave={() => {}}
  />
);
